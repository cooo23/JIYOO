Int 쓰면 좋은점
4byte기 때문에 속도가 가장 빠르다. 컴터가 4byte순으로 끊는다.

final이라고 선언 먼저 하고 값을 할당해도 무관
but 두번째부터는 값이 고정되기 때문에 에러가 발생한다.

---------------------------------------------------------------------------------------------------------------------------------------------
        f = 10.1f;
        d = 10.1;

        double d2 = f*f;
        double d3 = (double)f*f;
          float f2 = (float)(d*d);

        float f3 = (float)((float)d *d);
        //float f3 = (float)d *d); 에러 터지는 이유 : double형이 되는데 float이미 선언한게 다시 double형이 됨. 그리고 결과값 보니 float값이라 에러
----------------------------------------------------------------------------------------------------------------------------------------------
int n1 = 10, n2 = 3, n3, n4, n5;
선언할 떄 같은 형이면 콤마로 선언가능, 수 없이 선언만 할 수도 있음

String str= = "10" + 10 + 10;
앞의 문자열부터 계산하기 때문에 결과가 101010이된다
String str= = "10" + (10 + 10);
괄호를 묶어주면 우선순위가 되어서 1020이 된다.
-----------------------------------------------------------------------------------------------------------------------------------------------
https://gbs1995.tistory.com/23?category=797665
이것이 자바다 확인문제
https://drunkenpolarbear.tistory.com/18
이것이 자바다 교재파일
-----------------------------------------------------------------------------------------------------------------------------------------------
